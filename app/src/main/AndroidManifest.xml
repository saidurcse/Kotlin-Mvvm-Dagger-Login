<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="saidur.demo.kotlin">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="saidur.demo.kotlin.DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name="saidur.demo.kotlin.view.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="saidur.demo.kotlin.view.login.LoginActivity">
        </activity>
        <activity android:name="saidur.demo.kotlin.view.signup.SignupActivity">
        </activity>
        <activity android:name="saidur.demo.kotlin.view.landing.LandingActivity">
        </activity>
        <activity android:name="saidur.demo.kotlin.view.weather.WeatherActivity">
        </activity>
    </application>

</manifest>